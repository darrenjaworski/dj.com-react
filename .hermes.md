# darrenjaworski.com (dj.com-react)

Hermes: see [`AGENTS.md`](./AGENTS.md) for project rules (stack, structure,
commands, conventions, deploy). This file exists only because `.hermes.md`
takes discovery priority over `AGENTS.md` in Hermes — keep it as a pointer,
not a second copy, so there's one source of truth to maintain.
