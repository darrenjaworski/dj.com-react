version: 0.2

phases:
  install:
    runtime-versions:
      nodejs: 22
    commands:
      - npm i
  build:
    commands:
      - npm run build
artifacts:
  files:
    - "**/*"
  discard-paths: no
  base-directory: dist
